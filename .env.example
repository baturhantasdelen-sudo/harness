# Nexus Shield Harness — local evaluation (mock placeholders only)

# HTTP adapter: agent runtime endpoint under test
HARNESS_BASE_URL=http://127.0.0.1:8080

# Optional bearer token for your local agent gateway (never commit real secrets)
HARNESS_API_KEY=your_local_eval_token_here

# Synthetic agent identity for scenario runs
HARNESS_AGENT_ID=harness-agent-01

# Python HTTP benchmark uses the same variables (HARNESS_BASE_URL, HARNESS_API_KEY, HARNESS_AGENT_ID)
